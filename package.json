{
  "name": "slow-navigator",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.18.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "pnpm -r --filter \"./apps/*\" build",
    "dev": "pnpm --filter @slownav/hike dev"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "jsdom": "^30.1.0",
    "typescript": "~5.9.3",
    "vitest": "^5.0.1"
  }
}
